import 'package:matchito_annotation/matchito_annotation.dart';
import 'package:test/test.dart';

void main() {
  group('Matchito', () {
    test('()', () {
      expect(Matchito(), isNotNull);
    });
  });
}
